# th-cricket (monorepo)

This repository contains a TypeScript React client and a Node.js Express server.

Structure:
- client/ - Vite + React + TypeScript
- server/ - Express + TypeScript

Quick start:

1. Install dependencies for both:
   - cd client && npm install
   - cd ../server && npm install

2. Start in development:
   - cd client && npm run dev
   - cd server && npm run dev

If you want, I can add a top-level script and optionally install dependencies now.
