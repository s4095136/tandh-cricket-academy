# th-cricket Server

Node.js + Express server in TypeScript.

Quick start:

1. cd server
2. npm install
3. npm run dev

This creates a minimal Express server. Add routes and controllers under `src/`.
